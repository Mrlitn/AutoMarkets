<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.main.activity">


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name="com.main.activity.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".MainActivity"></activity>

        <activity android:name=".CitysName">

        </activity>

        <activity android:name=".SearchCars"></activity>

        <activity android:name=".MyInfo"></activity>
        <activity android:name=".CarHistory"></activity>
        <activity android:name=".UserFeedback"></activity>
        <activity android:name=".JoinUs"></activity>
        <activity android:name=".AboutUs"></activity>

    </application>

</manifest>